# URL publique de l'API Django Ninja (health, fetch JSON — jamais de secrets dans PUBLIC_*)
PUBLIC_API_URL=http://localhost:8000

# URL publique Django (liens HTML vers console, login, back-office)
PUBLIC_DJANGO_URL=http://localhost:8000
